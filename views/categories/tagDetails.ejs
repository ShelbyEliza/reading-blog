<!DOCTYPE html>
<html lang="en">
  <%- include('../partials/head.ejs') %>
  <body>
    <%- include('../partials/header.ejs') %> <%- include('../partials/nav.ejs')
    %>

    <div class="content">
      <h2 class="page-title"> <%= title %></h2>
      <% if (blogsWithTag.length > 0) { for (var i = 0; i < blogsWithTag.length; i++ ) { %>
      <div class="content-box">
          <div class="content-box-heading">
            <a class="titleLink" href="../../blogs/blogDetails/<%= blogsWithTag[i].id %>">
              <h1 class="title"><%= blogsWithTag[i].bookTitle %></h1>
            </a>
            <a class="authorLink" href="../author/<%= blogsWithTag[i].authorID %>">
              <h2 class="author"><%= blogsWithTag[i].author %></h2>
            </a>  
          </div>
      </div>
      <% } } else { %>
        <p>There are no blogs with the <%= title %> tag.</p>
        <% } %>
    </div>  
  </body>
</html> 